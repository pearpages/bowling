import { LastFrame } from "./LastFrame";
describe("LastFrame", () => {
  describe("Should add rolls to the last frame", () => {
    let lastFrame: LastFrame;
    beforeEach(() => {
      lastFrame = new LastFrame();
    });
    it("X", () => {
      expect(true).toBe(true);
    });
  });
});
